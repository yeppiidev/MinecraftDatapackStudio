namespace MinecraftDatapackStudio.Constants {
    class TemplateTexts {
        public static class MCFunction {
            public static string Header = "# Example function\n/execute as @p at @s run say \"hello\"";
            public static string TickFunctionHeader = "# The commands here will be executed every tick (20 times a second)";
            public static string LoadFunctionHeader = "# The commands here will be executed when the datapack loads";
        }
    }
}
